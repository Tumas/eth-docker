x-logging: &logging
  logging:
    driver: ${LOG_DRIVER}
    options:
      max-size: ${LOG_MAX_SIZE}
      max-file: ${LOG_MAX_FILE}
      tag: ${LOG_TAG}


x-build: &gr-build
  context: ./grandine
  dockerfile: ${GRANDINE_DOCKERFILE:-Dockerfile.binary}
  args:
    - BUILD_TARGET=${GRANDINE_SRC_BUILD_TARGET:-master}
    - SRC_REPO=${GRANDINE_SRC_REPO:-https://github.com/sifraitech/grandine}
    - DOCKER_TAG=${GRANDINE_DOCKER_TAG:-latest}
    - DOCKER_REPO=${GRANDINE_DOCKER_REPO:-sifrai/grandine}

services:
  grandine-builder:
    restart: "no"
    build:
      <<: *gr-build
    image: grandine:local
    pull_policy: never
    entrypoint: ["true"]

  validator:
    restart: "unless-stopped"
    image: grandine:local
    pull_policy: never
    user: gdvalidator
    depends_on:
      grandine-builder:
        condition: service_completed_successfully
    volumes:
      - grandinevalidator-data:/var/lib/grandine/validator
      - /etc/localtime:/etc/localtime:ro
    environment:
      - CL_NODE=${CL_NODE:-http://consensus:5052}
      - MEV_BOOST=${MEV_BOOST}
      - EPBS_BUILD_FACTOR=${EPBS_BUILD_FACTOR}
      - MEV_NODE=${MEV_NODE}
      - DOPPELGANGER=${DOPPELGANGER:-false}
      - VC_EXTRAS=${VC_EXTRAS:-}
      - GRAFFITI=${GRAFFITI:-}
      - DEFAULT_GRAFFITI=${DEFAULT_GRAFFITI:-false}
      - WEB3SIGNER=${WEB3SIGNER:-false}
      - W3S_NODE=${W3S_NODE}
      - NETWORK=${NETWORK}
      - LOG_LEVEL=${LOG_LEVEL}
      - RUST_LOG=${LOG_LEVEL:-info}
    networks:
      default:
        aliases:
          - ${VC_ALIAS:-vc}
    <<: *logging
    entrypoint:
      - docker-entrypoint-vc.sh
      - grandine
      - vc
      - --data-dir
      - /var/lib/grandine/validator
      - --metrics
      - --metrics-address
      - 0.0.0.0
      - --metrics-port
      - "8009"
      - --suggested-fee-recipient
      - ${FEE_RECIPIENT}
      - --keystore-storage-password-file
      - /var/lib/grandine/validator/wallet-password.txt
      - --enable-validator-api
      - --validator-api-address
      - 0.0.0.0
      - --validator-api-port
      - ${KEY_API_PORT:-7500}
      - --validator-api-allowed-origins=*
    labels:
      - metrics.scrape=true
      - metrics.path=/metrics
      - metrics.port=8009
      - metrics.instance=validator
      - metrics.network=${NETWORK}
      - logs.collect=true

  validator-keys:
    profiles: ["tools"]
    restart: "no"
    build:
      context: ./vc-utils
    image: vc-utils:local
    pull_policy: never
    # The API token has 640 permissions. Root copies it,
    # then switches to the local user's UID or if not provided,
    # 1000. The UID has to be able to write .eth/validator_keys
    # for the "keys delete" command.
    user: root
    volumes:
      - grandinevalidator-data:/var/lib/grandine/validator
      - ./.eth/validator_keys:/validator_keys
      - ./.eth/exit_messages:/exit_messages
      - /etc/localtime:/etc/localtime:ro
    environment:
      - KEYSTORE_PASSWORD=${KEYSTORE_PASSWORD:-}
      - KEY_API_PORT=${KEY_API_PORT:-7500}
      - WEB3SIGNER=${WEB3SIGNER:-false}
      - W3S_NODE=${W3S_NODE}
      - CL_NODE=${CL_NODE}
    depends_on:
      - validator
    entrypoint:
      - keymanager.sh
      - /var/lib/grandine/validator/${NETWORK}/validator/api-token.txt
      - ${VC_ALIAS:-vc}

volumes:
  grandinevalidator-data:

networks:
  default:
    enable_ipv6: ${IPV6:-false}
